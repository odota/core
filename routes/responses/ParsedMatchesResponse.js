import { match_id as _match_id } from '../../properties/commonProperties';

export const ParsedMatchesResponse = {
  title: 'ParsedMatchesResponse',
  type: 'object',
  properties: {
    match_id: _match_id,
  },
};
