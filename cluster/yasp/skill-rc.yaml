apiVersion: v1
kind: ReplicationController
metadata:
  labels:
    tier: backend
    deployment: yasp
  name: skill-1
  namespace: yasp
spec:
  replicas: 1
  selector:
    app: skill
    tier: backend
  template:
    metadata:
      labels:
        app: skill
        tier: backend
    spec:
      containers:
      - name: yasp
        image: yasp/yasp:8c3dece
        args: [ "node", "skill.js" ]
        imagePullPolicy: Always
      restartPolicy: Always