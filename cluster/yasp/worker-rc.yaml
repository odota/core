apiVersion: v1
kind: ReplicationController
metadata:
  labels:
    tier: backend
    deployment: yasp
  name: worker-1
  namespace: yasp
spec:
  replicas: 1
  selector:
    app: worker
    tier: backend
  template:
    metadata:
      labels:
        app: worker
        tier: backend
    spec:
      containers:
      - name: yasp
        image: yasp/yasp:8c3dece
        args: [ "node", "worker.js" ]
        imagePullPolicy: Always
      restartPolicy: Always