apiVersion: v1
kind: Service
metadata:
  labels:
    tier: backend
  name: web
spec:
  selector:
    app: web
    tier: backend
  ports:
  - nodePort: 30246
    port: 80
    targetPort: web
  type: NodePort