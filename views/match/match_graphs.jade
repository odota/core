extend match
    
block match_content
  .row
    .col-md-12
      h2 Graphs
      .alert.alert-success.text-center Click on an series in the legend to select/deselect.  Alt-click to select only one.
      h3 Radiant Advantage
      #chart-diff
      h3 Gold
      #chart-gold
      hr
      +ad("banner3")
      hr
      h3 XP
      #chart-xp
      h3 LH
      #chart-lh

append footer_assets
  script.
    var graphData = !{JSON.stringify(match.graphData)};
    generateCharts(graphData);