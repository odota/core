extend player

block playercontent
  .row
    .col-md-12
      h3 Rating 
        small To display MMR, share it on your in-game Dota profile.
      div.btn.btn-info.btn-sm(onclick='toggle()') Toggle Timeseries
      #ratings
      

append footer_assets
  script.
    var ratings = !{JSON.stringify(player.ratings)};
    var indexBased = false;
    var toggle = function(){
      indexBased = !indexBased;
      ratingsChart(ratings, indexBased);
    };
    ratings.push({
    time: new Date().setDate(new Date().getDate() + 7),
    solo_competitive_rank: 7000,
    competitive_rank: null
     });
         ratings.push({
    time: new Date().setDate(new Date().getDate() + 14),
    solo_competitive_rank: 7000,
    competitive_rank: 4000
     });
    ratingsChart(ratings, indexBased);
