extend player

include /mixins/hero_table
include /mixins/teammate_table
include /mixins/match_table

block playercontent
  //pre=JSON.stringify(player.aggData)

  .row
    .col-md-6
      +match_table(player.data.slice(0,20))
    .col-md-6
      +hero_table(player.heroes_list.slice(0,10))
      +teammate_table(player.teammate_list.slice(0,10))
      
append footer_assets
  script.
